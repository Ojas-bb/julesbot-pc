# julesbot-pc
## Backend Server

The application requires a backend server to fetch session data. A simple Python script is provided to mock this backend.

To run the server:
```bash
python server.py
```

The server will start on port 8080.
